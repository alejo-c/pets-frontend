type Adopter = {
    id: string
    name: string
    address: string
    contact: string
}

export const emptyAdopter = {
    id: '',
    name: '',
    address: '',
    contact: ''
}

export default Adopter